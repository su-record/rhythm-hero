# Habit Toy 캐릭터 중심 UI 리뉴얼 기획서

> 문서 상태: 2026-08-20 기준 1차 디자인·구현 완료  
> 생성형 이미지와 Higgsfield는 사용하지 않았으며, 첨부 원본 PNG 2개를 그대로 적용했다.  
> 작성 기준: 현재 실행 화면, 실제 코드 동작, 첨부 PNG 2개, [Dribbble Routine Tracker 레퍼런스](https://dribbble.com/shots/23700574-Routine-Tracker-for-Kids-Mobile-IOS-App)

워크스페이스의 제품 명세서는 기능 의도를 이해하기 위한 참고 자료로만 사용한다. 명세에 적힌 미래 M1/M2 기능은 이번 요청의 지시로 간주하지 않으며, **현재 실제 코드 동작과 사용자의 “기능 유지” 요청을 우선 기준**으로 삼는다.

## 1. 결론

이번 리뉴얼의 방향은 **“밝은 색을 더하는 것”이 아니라 “캐릭터가 기록 상태에 반응하는 촉각적인 시간 도구”로 바꾸는 것**이다.

Dribbble 레퍼런스에서 가져올 요소는 다음 네 가지다.

1. 큰 라운드 패널과 굵은 외곽선으로 만든 장난감 같은 구조
2. 고채도 색상 면을 이용한 명확한 화면 위계
3. 캐릭터를 장식이 아니라 현재 상태를 알려주는 피드백으로 사용하는 방식
4. 짧고 즉각적인 눌림·통통 튀는 반응

반면 레퍼런스의 코인, 트로피, 완료 경쟁 같은 보상 체계는 가져오지 않는다. Habit Toy는 목표 달성 여부로 사용자를 평가하지 않는 제품이며, **1분 미만의 기록도 가치 있게 보존하는 현재 기능과 메시지**를 유지해야 하기 때문이다.

초기 구현에는 Higgsfield가 필요하지 않다. 첨부된 투명 PNG와 CSS `transform` 애니메이션만으로 더 가볍고 일관된 인터랙션을 만들 수 있다. Higgsfield는 나중에 온보딩용 짧은 영상이나 추가 표정/포즈가 꼭 필요해졌을 때만 선택적으로 검토한다.

## 2. 현재 상태 진단

### 2.1 잘 되어 있는 점

- 오늘 총 시간, Active 4, 기록, 리플렉션, 설정의 정보 구조가 명확하다.
- 모바일 우선 최대 720px 레이아웃과 하단 내비게이션이 안정적이다.
- 기록 시작·종료·전환, 수동 기록, 수정·삭제·복구, 7/30일 히트맵, AI 리플렉션, USB 연결까지 핵심 기능이 이미 작동한다.
- 색상 외 텍스트 상태, 44px 터치 영역, `prefers-reduced-motion` 등 접근성 원칙이 제품 명세에 정의되어 있다.
- 현재 기준 `npm run check`와 `npm test`가 통과한다.

### 2.2 단조롭게 느껴지는 원인

- 화면 대부분이 크림에 가까운 배경과 흰 카드, 얇은 회색 테두리로 구성되어 섹션 간 성격 차이가 작다.
- 파스텔 토큰은 존재하지만 작은 도트·진행 막대·번호 배지에만 사용되어 브랜드 인상으로 연결되지 않는다.
- 브랜드 마크가 작은 2×2 파스텔 사각형이라 캐릭터 에셋과 시각적 연관이 없다.
- Active 4 카드 네 개가 거의 같은 흰 상자로 보여, 앱의 핵심 입력 장치인데도 촉각적인 인상이 약하다.
- 시작·종료 피드백이 텍스트, 작은 점, 토스트 중심이라 캐릭터가 제공할 수 있는 감정적 반응이 비어 있다.
- repo에 캐릭터 에셋이 있지만 현재 HTML/CSS/JS 어디에서도 사용되지 않는다.

## 3. 범위와 보존 원칙

### 3.1 이번 리뉴얼 범위

- 전체 색상, 타이포 위계, 외곽선, 라운드, 간격, 카드 표면 재설계
- 첨부 핑크·블루 캐릭터의 화면별 배치
- 기록 상태에 따른 캐릭터와 카드의 마이크로인터랙션
- 오늘, 기록, 리플렉션, 설정, 다이얼로그, 토스트, 하단 내비게이션의 일관된 리스킨
- 새 에셋의 PWA 캐시, MIME, 반응형·접근성·성능 처리

### 3.2 하지 않는 것

- 정보 구조, 탭 수, 데이터 모델, 기록 방식 변경
- 코인, 점수, 랭킹, 스트릭, 벌점, 미달 경고 등 새로운 게임 기능 추가
- Category 색상을 브랜드 핑크·블루로 강제 변경
- AI 리플렉션의 입력·출력 규칙 변경
- Web Serial 프로토콜이나 하드웨어 LED 의미 변경
- Dribbble 화면의 픽셀 단위 복제 또는 해당 캐릭터·타이포의 모사
- 현재 단계의 이미지 생성, 영상 생성, 구현 코드 수정

### 3.3 반드시 보존할 기능 계약

| 영역 | 보존할 동작 |
|---|---|
| 기록 | 같은 Active 4 버튼은 시작/종료, 다른 버튼은 즉시 전환 |
| 입력 | 화면 카드, 키보드 1–4, `window.habitToy.pressButton(1..4)`, Web Serial |
| 시간 | 실행 중 타이머 1초 갱신, 자정 분할 집계, `1분 미만` 표시 |
| 데이터 | `localStorage` 우선, 서버 동기화, 오프라인 복구, JSON 내보내기 |
| 기록 관리 | 수동 추가, 수정, 삭제, 30일 내 복구 |
| Category | 추가, 보관, 복원, 목표 수정, Active 4 교체 시 과거 기록 유지 |
| 통계 | 7일/30일 전환, 히트맵, Category 상세, 최근 기록 |
| 리플렉션 | 근거 기반 로컬 카드, 선택적 AI 카드, AI 실패 시 기능 유지 |
| 기기 | USB 연결/해제, 테스트, LED 상태 동기화 |
| PWA | 서비스 워커 캐시, 오프라인 실행, safe-area 대응 |

구현 시 기존 HTML `id`와 `data-*` 속성은 DOM API 계약으로 취급한다. 특히 `data-tab`, `data-range`, `data-button-index`, `data-session-id`, `data-category-detail`, `data-assignment`, `data-goal`, `data-archive-category`, `data-restore-session`, `data-active4-slot`을 유지한다.

## 4. 디자인 콘셉트

### 4.1 콘셉트 문장

작업명: **Spike Garden**

**“쌓이는 시간, 반응하는 친구들.”**

기록 전에는 핑크 캐릭터가 조용히 기다리고, 기록 중에는 블루 캐릭터가 에너지 있게 반응한다. 사용자가 목표를 채웠는지보다 **지금 기록하고 있다는 사실과 실제로 쌓인 시간**을 캐릭터가 함께 보여준다.

### 4.2 시각 키워드

- playful, tactile, hand-drawn
- bright but readable
- imperfect, friendly, non-judgmental
- bold outline, soft cream, electric accent
- 한 화면에 큰 캐릭터는 하나만 배치해 데이터 가독성 유지

### 4.3 레퍼런스에서의 번역 방식

| 레퍼런스 특징 | Habit Toy 적용 방식 |
|---|---|
| 연두·초록의 큰 색상 면 | 크림 바탕 위 라임·핑크·블루 포인트 패널 |
| 굵은 검정 테두리 | 핵심 카드와 CTA에 2px 잉크 외곽선 |
| 펫 중심 홈 | 오늘 누적 시간과 실행 상태를 캐릭터가 감싸는 히어로 |
| 둥근 태스크 행 | Active 4를 눌리는 장난감 버튼 카드로 표현 |
| 코인·트로피 | 도입하지 않음. 시간·근거·기기 상태만 표시 |
| 장난스러운 글꼴 | 한글 가독성은 유지하고 제목·숫자에서만 둥글고 굵은 인상 강화 |

## 5. 비주얼 시스템

### 5.1 컬러 토큰 초안

브랜드 색상과 Category 색상은 별도 계층으로 관리한다. Category 색상은 통계와 하드웨어 LED 데이터이므로 덮어쓰지 않는다.

| 토큰 | 값 | 용도 |
|---|---:|---|
| `--ink` | `#1B1721` | 본문, 외곽선, 아이콘, 밝은 색상 면 위 텍스트 |
| `--canvas` | `#FFF7E8` | 전체 배경 |
| `--surface` | `#FFFCF5` | 카드와 시트 |
| `--pink` | `#DC99A4` | 첨부 핑크 캐릭터와 조용한 상태 |
| `--blue` | `#00C2FF` | 첨부 블루 캐릭터와 실행 상태 |
| `--lime` | `#E4FF57` | 주요 히어로, 선택, 핵심 CTA 강조 |
| `--green` | `#53D477` | 연결 성공, 보조 패널 |
| `--yellow` | `#FFD85A` | 따뜻한 하이라이트 |
| `--danger` | `#A93651` | 삭제와 오류 텍스트 전용 |
| `--line` | `#1B1721` | 핵심 카드 2px 외곽선 |
| `--line-soft` | `rgba(27,23,33,.16)` | 목록 구분선 |

사용 규칙:

- 핑크와 블루 위에는 흰색 글자를 쓰지 않고 `--ink`를 사용한다.
- 상태는 색만으로 전달하지 않고 `기록 중`, `연결됨`, `달성` 같은 텍스트와 형태를 함께 쓴다.
- Category 카드의 진행색은 기존 `--category`를 유지하고, 브랜드 배경은 `--brand-*` 토큰만 사용한다.
- 한 화면에서 강한 색상 면은 2개 이하로 제한해 캐릭터와 데이터가 경쟁하지 않게 한다.

### 5.2 형태와 깊이

- 핵심 패널 radius: 26–30px
- 일반 카드 radius: 20–24px
- 작은 배지·입력 radius: 12–16px
- 핵심 카드: 2px `--ink` 외곽선
- 일반 목록: 1px `--line-soft`
- 선택적 장난감 그림자: `4px 4px 0 var(--ink)`
- 그림자는 히어로, 주요 CTA, 선택 카드에만 사용한다.

### 5.3 타이포그래피

- 본문은 현재 Pretendard/SUIT/system stack을 유지해 한글 가독성과 오프라인 안정성을 확보한다.
- 제목은 750–850 굵기, 자간은 현재의 과도한 압축을 완화한다.
- 오늘 누적 시간은 가장 큰 숫자 위계를 유지하되 캐릭터와 겹치지 않도록 최대 두 줄 영역을 예약한다.
- 레퍼런스의 손글씨 느낌은 폰트 전체 교체가 아니라 작은 라벨, 말풍선 꼬리, 기울기 1–2도 같은 CSS 디테일로 먼저 구현한다.
- 별도 디스플레이 폰트는 한글 지원·라이선스·오프라인 self-host 조건을 확인한 뒤 2차 선택 사항으로 둔다.

### 5.4 배경 그래픽

- CSS 원·점·짧은 사선으로 만든 저비용 패턴을 히어로 모서리에만 사용한다.
- 캐릭터 얼굴 뒤에 텍스트를 얹지 않는다.
- 캐릭터 실루엣 전체를 배경 타일로 반복하지 않는다.
- 스파이크가 카드 밖으로 살짝 나오는 `peek` 연출은 허용하되 주요 터치 영역과 스크롤바를 가리지 않는다.

## 6. 캐릭터 사용 체계

### 6.1 원본 기준

구현 시 사용자가 첨부한 두 파일을 원본 소스로 삼는다.

| 에셋 | 크기 | 주색 | 기본 역할 |
|---|---:|---:|---|
| `pink-spike-cutout.png` | 553×475 RGBA | `#DC99A4` | 대기, 안내, 빈 상태, 리플렉션 |
| `blue-spike-cutout.png` | 762×731 RGBA | `#00C2FF` | 시작, 실행 중, 전환, 즉각 반응 |

repo의 기존 `assets/characters/bouncer-*.png`는 첨부 원본과 크기·해시가 다르므로 자동 대체하지 않는다. 구현 시작 시 첨부 원본을 canonical source로 복사하고, 기존 변형은 시각 비교가 끝날 때까지 보존한다.

### 6.2 상태 문법

| 상태 | 캐릭터 | 표현 | 텍스트 동반 |
|---|---|---|---|
| 첫 진입·대기 | 핑크 | 느린 3–4px 부유 또는 정지 | “아직 기록을 기다리고 있어요.” |
| 기록 시작 | 블루 | 240ms pop-in + 선택 카드 눌림 | “기록 중” |
| 기록 중 | 블루 | 작은 상하 bob, 타이머와 분리 | Category명 + 경과 시간 |
| 다른 Category로 전환 | 블루 | 짧은 좌우 spring | 이전 종료/새 기록 시작 토스트 |
| 기록 종료 | 핑크 복귀 | 블루 1회 pop 후 페이드 | 실제 기록 시간 |
| 빈 기록/리플렉션 | 핑크 | 정지 또는 아주 느린 부유 | 다음에 무엇이 나타날지 설명 |
| 연결 테스트 | 블루 | 한 번 튀는 반응 | 연결/데모 상태 텍스트 |

캐릭터는 성취 여부를 심판하는 표정으로 사용하지 않는다. 목표 미달 상태에 실망, 빨강 경고, 시무룩한 반응을 배정하지 않는다.

여러 상태가 겹치면 `열린 다이얼로그 → 기록 중 → 방금 완료 → 화면 기본 상태` 순서로 표현한다. 우선순위를 고정해 캐릭터가 깜빡이거나 상반된 반응을 동시에 보이지 않게 한다.

### 6.3 크기와 크롭

- 기본은 `object-fit: contain`; 얼굴과 스파이크 전체를 보존한다.
- 모바일 히어로: 핑크 약 130–170 CSS px, 블루 약 140–190 CSS px
- 카드 보조: 72–112 CSS px
- 48px 이하 아이콘으로 축소하지 않는다. 작은 크기에서는 얼굴 선이 뭉개진다.
- 핑크는 가로형이라 블루와 나란히 배치할 때 8–12% 더 크게 optical sizing한다.
- 애니메이션 wrapper에 외곽 8–12% 여유를 두어 스파이크가 잘리지 않게 한다.
- 블루 원본은 하드한 알파 경계이므로 회전·축소 시 shimmering이 없는지 실제 기기에서 확인한다.

### 6.4 접근성

- 순수 장식은 `alt=""`와 `aria-hidden="true"`를 사용한다.
- 상태 전달에 관여하면 색 이름이 아닌 의미 중심 레이블을 사용한다. 예: `aria-label="독서 기록 중"`.
- 모션 감소 설정에서는 캐릭터를 정지시키고 상태 배지·외곽선·텍스트만 바꾼다.
- 핑크와 블루 자체의 명도 차이가 작으므로 두 캐릭터만으로 상태를 구분하지 않는다.

## 7. 화면별 설계

### 7.1 공통 헤더와 하단 내비게이션

헤더:

- 브랜드명 `Habit Toy`는 유지한다.
- 2×2 컬러 블록 대신 CSS로 만든 작은 방사형 burst 마크를 사용한다. 실제 캐릭터 PNG는 48px 이하 로고로 쓰지 않는다.
- 동기화 문구는 데스크톱에서 유지하고, 모바일의 점 상태는 기존처럼 남긴다.
- 기기 상태 칩은 크림 표면 + 굵은 외곽선의 작은 물리 버튼 형태로 바꾼다.

하단 내비게이션:

- 현재 4탭과 레이블은 그대로 둔다.
- 바 전체를 크림 시트로 만들고 활성 탭은 라임 캡슐 또는 손그림 밑줄로 표시한다.
- 아이콘과 레이블을 함께 유지해 색상 의존을 피한다.
- safe-area와 최소 44px 터치 영역을 유지한다.

### 7.2 오늘

#### 히어로

- 현재 분리된 `오늘의 기록`, 큰 시간, 상태 문구를 하나의 라임 또는 크림 아웃라인 패널로 묶는다.
- 왼쪽에는 날짜와 누적 시간, 오른쪽 아래에는 핑크 캐릭터를 배치한다.
- 기록이 시작되면 패널이 블루 계열 보조 배경으로 바뀌고 핑크 대신 블루 캐릭터가 나타난다.
- 현재 `now-card`의 Category명, 타이머, 종료 버튼은 히어로 아래 또는 내부의 고대비 인셋 패널로 통합한다.
- 타이머는 캐릭터 애니메이션과 독립된 DOM에 둬 1초 갱신에도 움직임이 재시작되지 않게 한다.

#### Active 4

- 2×2 구조를 기본으로 유지한다.
- 각 카드는 2px 외곽선, Category색 상단 탭, 큰 번호, 이름, 실제 시간, 진행 트랙을 갖는 눌리는 장난감 버튼으로 바꾼다.
- 전체 카드가 기존처럼 하나의 `<button>`이어야 하며 내부에 별도 버튼을 중첩하지 않는다.
- 기본 상태에는 `시작`, 실행 상태에는 `기록 중`과 파형/점 아이콘을 함께 표시한다.
- 누를 때 80–120ms 아래로 2px 이동하고 그림자가 줄어 실제 버튼 같은 피드백을 준다.

#### 수동 기록과 미니 리플렉션

- `앱에서 직접 기록`은 점선 또는 크림 보조 버튼으로 유지해 Active 4보다 위계가 낮게 보이게 한다.
- 미니 리플렉션은 핑크 캐릭터가 옆에서 말하는 말풍선 카드로 바꾼다.
- `자세히`는 텍스트 링크가 아니라 충분한 터치 크기의 작은 캡슐 버튼으로 만든다.

### 7.3 기록

- 7일/30일 segmented control은 굵은 외곽선과 라임 선택 상태로 바꾼다.
- 기간 요약 세 칸은 하나의 흰 표가 아니라 서로 다른 높이의 장난감 블록처럼 보이게 하되 숫자 위계는 유지한다.
- 히트맵은 Category의 기존 색상 데이터를 그대로 사용하고, 0분 셀과 1분 이상 셀이 명확히 달라야 한다.
- 셀에는 색 외에도 작은 점/테두리 차이를 추가해 색각 이상 사용자를 지원한다.
- 30일 가로 스크롤과 현재 날짜 외곽선은 유지한다.
- 데이터가 있을 때는 캐릭터를 크게 넣지 않는다. 빈 상태 또는 Category 상세 상단에만 핑크 캐릭터를 72–96px로 제한한다.
- 최근 기록 목록은 카드 남발 대신 굵은 섹션 외곽선 안의 행 구조를 유지해 정보 밀도를 보호한다.

### 7.4 리플렉션

- 화면 상단에 핑크 캐릭터와 `기록이 들려주는 이야기` 말풍선을 배치한다.
- 일반 리플렉션은 크림/핑크 톤, AI 보조 카드는 블루 톤의 얇은 상단 탭으로 구분한다.
- `기록 기반`, 기간, 표본 수 등 근거 영역은 색상 면을 분리해 카드 본문과 명확히 구분한다.
- AI 버튼은 마법·정답처럼 과장하지 않고 현재의 보조 기능 위계를 유지한다.
- 평가·명령·성취 점수처럼 보이는 별점이나 메달 표현은 사용하지 않는다.

### 7.5 설정

- 설정 기능은 많으므로 캐릭터보다 구조적 색상 블록을 우선한다.
- `Active 4 배치`, `일일 목표`, `모든 Category`, `최근 삭제`, `기기`, `내 데이터`를 각각 독립된 아웃라인 섹션으로 구분한다.
- 번호 배지는 Active 4 카드와 동일한 형태를 재사용한다.
- 입력 필드와 select는 최소 높이 44px, 명확한 포커스 링, 잉크 외곽선을 갖는다.
- USB/데모 기기 패널에는 블루 캐릭터가 한 번 반응할 수 있으나 72–96px 이하의 보조 요소로 둔다.
- 삭제, 초기화는 브랜드 핑크와 혼동하지 않도록 별도 danger 토큰과 명시적 텍스트를 사용한다.

### 7.6 다이얼로그·토스트

- 모바일에서는 현재 bottom sheet 동작을 유지한다.
- 상단에 짧은 컬러 탭이나 손그림 grabber를 추가하고, 본문은 크림 표면 + 2px 외곽선으로 통일한다.
- 주요 버튼은 라임, 보조 버튼은 크림, 삭제 버튼은 danger 텍스트/테두리로 구분한다.
- 토스트는 검정 캡슐 대신 캐릭터 말풍선처럼 보이는 잉크 외곽선 크림 패널로 바꾸되 `aria-live`를 유지한다.
- 캐릭터를 다이얼로그마다 반복하지 않는다. 기록 완료·연결 테스트처럼 피드백 가치가 큰 경우만 사용한다.

## 8. 인터랙션 사양

| 인터랙션 | 시간 | 동작 |
|---|---:|---|
| 카드 hover | 160ms | 1px 상승, 외곽선 유지 |
| 카드 press | 80–120ms | `translateY(2px) scale(.99)`, 그림자 축소 |
| 기록 시작 캐릭터 | 240ms | 블루 `scale(.88→1.04→1)` |
| 기록 중 bob | 1.8–2.4s | `translateY(0→-4px→0)`, 큰 회전 금지 |
| 기록 종료 | 320ms | 블루 1회 pop 후 핑크로 전환 |
| 탭 전환 | 180–220ms | opacity + 4px 이동 |
| 진행 막대 | 250ms | width 전환 |
| bottom sheet | 220ms | 기존 아래→위 진입 유지 |

원칙:

- 애니메이션은 `transform`과 `opacity` 중심으로 구현한다.
- 계속 도는 모션은 기록 중 캐릭터 하나에만 허용한다.
- 캐릭터 모션이 버튼 입력 피드백보다 먼저 느껴져서는 안 된다.
- `prefers-reduced-motion: reduce`에서는 반복 모션과 spring을 제거한다.
- 소리, 자동 재생 영상, 진동은 이번 범위에 추가하지 않는다.

## 9. 기술 구현 계획

### 9.1 구현 전략

**CSS 중심 + HTML에 장식 레이어 추가 + JavaScript 최소 변경**을 기본으로 한다. 새 프레임워크나 애니메이션 라이브러리는 도입하지 않는다.

#### `styles.css`

- 기존 토큰을 브랜드/Category/상태 토큰으로 재구성
- 공통 panel, toy-card, badge, speech-bubble, character-wrapper 스타일 추가
- 오늘·기록·리플렉션·설정·다이얼로그·내비게이션 리스킨
- 모바일/데스크톱/320px 대응과 reduced-motion 정의

#### `index.html`

- 오늘 히어로와 리플렉션 헤더에 캐릭터용 정적 wrapper 추가
- 장식 이미지는 `alt=""`, 상태 이미지에는 의미 중심 레이블 제공
- 기존 `id`, 버튼, form, dialog 구조는 유지
- CSS/JS 캐시 버전 갱신

#### `app.js`

- 기존 상태 구조와 저장 형식은 변경하지 않음
- 가능하면 현재 `body.is-running` 클래스만으로 캐릭터 상태 전환
- 상태 텍스트가 필요할 때만 `data-state` 또는 클래스 최소 추가
- 사용자 Category명 삽입 시 `escapeHtml`/`textContent` 사용
- 시각 효과를 위해 영속 데이터 필드를 추가하지 않음

#### 에셋

- 첨부 원본을 repo 내부 `assets/characters/`에 canonical 파일명으로 복사
- `width`/`height` 또는 `aspect-ratio`를 지정해 레이아웃 시프트 방지
- PNG 총 용량이 약 51KiB로 작으므로 우선 그대로 사용
- JPEG 변환 금지; WebP/AVIF 무손실 파생본은 실제 측정 후에만 도입

#### `server.mjs`, `sw.js`, `manifest.webmanifest`

- PNG/WebP/SVG/폰트 MIME 타입 보강
- 캐릭터 파일을 PWA precache 목록에 추가
- 서비스 워커 캐시명과 `?v=` 버전을 함께 갱신
- manifest와 HTML의 theme/background color를 새 canvas 색상으로 통일

### 9.2 중요한 렌더링 위험

현재 실행 중에는 `renderToday()`가 매초 `category-grid.innerHTML`을 다시 만든다. 카드 안에 장기 애니메이션이나 캐릭터를 넣으면 1초마다 재시작되어 끊겨 보일 수 있다.

우선안:

1. 큰 캐릭터를 재생성되지 않는 정적 히어로 DOM에 둔다.
2. 기존 `body.is-running`으로 핑크/블루 노출만 전환한다.
3. 카드 애니메이션은 클릭 순간의 짧은 CSS `:active` 반응만 사용한다.

필요할 때의 개선안:

- `renderToday()`를 구조 렌더와 시간 텍스트 갱신으로 분리한다.
- 1초 interval에서는 타이머, 누적 시간, 진행률만 부분 업데이트한다.
- 이 리팩터링은 화면 결과를 바꾸지 않는 범위에서 별도 커밋으로 수행한다.

## 10. Higgsfield 사용 판단

### 10.1 1차 구현

사용하지 않는다.

이유:

- 두 캐릭터의 손그림 실루엣과 얼굴이 이미 완성되어 있다.
- 필요한 반응은 bob, wobble, squash, pop 수준으로 CSS가 더 선명하고 가볍다.
- 생성형 영상은 스파이크 수, 눈·입 위치, 선 굵기가 프레임마다 흔들릴 위험이 있다.
- 영상은 reduced-motion, 반응형 크롭, PWA 오프라인 캐시, 초기 로딩 관리가 더 복잡하다.
- 기능 상태와 정확히 동기화되는 인터랙션은 생성형 영상보다 DOM/CSS가 적합하다.

### 10.2 선택적 사용 조건

다음 중 하나가 확정될 때만 2차로 검토한다.

- 별도의 온보딩 또는 런칭용 2–3초 장면이 필요함
- CSS로 만들기 어려운 추가 표정/포즈 세트가 필요함
- 브랜드 캠페인용 비기능성 영상이 필요함

사용할 경우 고정해야 할 조건:

- 원본 주색, 검정 얼굴선, 불규칙한 스파이크, 2D 비사진 스타일 유지
- 배경과 UI를 생성물에 합성하지 않고 캐릭터만 분리
- 자동 재생 소리 없음
- 정지 PNG fallback 제공
- loop 시작/끝 프레임 일치, 형태 morphing 여부 수동 검수
- 핵심 시작/종료 피드백은 영상과 무관하게 DOM 텍스트로 즉시 제공

## 11. 반응형·접근성·성능 기준

### 11.1 반응형

- 기준 화면: 320×568, 375×812, 390×844, 430×932, 720px 셸
- 360px 이상: Active 4 2열
- 359px 이하: 1열 또는 텍스트가 잘리지 않는 compact 2열 중 실제 QA로 결정
- 하단 내비게이션과 bottom sheet는 iOS safe-area 유지
- 30일 히트맵은 장식 요소 없이 가로 스크롤 가능해야 함

### 11.2 접근성

- 모든 주요 터치 영역 44×44px 이상
- 본문/레이블/상태 텍스트 WCAG AA 대비 확인
- 색상 + 텍스트 + 아이콘/패턴의 복수 신호 사용
- 키보드 탭 순서와 `focus-visible` 링 유지
- `aria-live`인 타이머 상태·토스트 문구 유지
- decorative image는 스크린리더에서 숨김
- reduced-motion에서 기능 및 상태 이해가 동일해야 함

### 11.3 성능

- 첨부 PNG 총 전송량 약 51KiB, 디코드 메모리 약 3.13MiB
- 같은 파일을 여러 번 복제하기보다 브라우저 캐시 재사용
- 화면 아래 캐릭터는 `loading="lazy"`, 실행 직전 필요한 블루 캐릭터는 preload 검토
- 이미지 영역 크기를 미리 예약해 CLS 방지
- 애니메이션은 layout 속성이 아닌 transform/opacity 사용
- 모바일에서 장시간 타이머 실행 중 메모리 증가와 프레임 드롭 확인

## 12. 구현 단계

### Phase 0. 기준선 고정

- 현재 네 화면과 다이얼로그의 데스크톱/모바일 스크린샷 저장
- 기존 기능 체크리스트와 DOM ID/data 계약 기록
- `npm run check`, `npm test` 통과 상태 보존

### Phase 1. 토큰과 공통 컴포넌트

- 컬러, radius, outline, shadow, type scale 토큰 적용
- 공통 버튼, 카드, 배지, 입력, 말풍선, 내비게이션 리스킨
- 데이터·JS 동작 변경 없이 시각 레이어만 검증

### Phase 2. 오늘 화면과 캐릭터 상태

- 히어로 패널과 핑크/블루 캐릭터 배치
- idle/running 전환과 Active 4 버튼 피드백 구현
- 1초 렌더링에도 캐릭터 모션이 재시작되지 않는지 확인

### Phase 3. 기록·리플렉션·설정·다이얼로그

- 화면별 색상 위계와 컴포넌트 적용
- 히트맵 스크롤, 긴 Category명, 빈 상태, 오류 상태 검증
- 캐릭터 밀도를 제한해 데이터 가독성 확인

### Phase 4. PWA와 에셋

- MIME, service worker precache, cache version, manifest 색상 갱신
- 오프라인 최초/재방문 로드 검증
- 필요 시에만 무손실 파생 포맷 비교

### Phase 5. 회귀·접근성 QA

- 기능 매트릭스 전체 수동 검증
- 키보드, 스크린리더 레이블, reduced-motion, 색상 대비 확인
- 320/390/720px 및 모바일 Safari/Chrome에서 시각 회귀 확인
- 기존 자동 테스트와 정적 에셋/MIME smoke test 실행

## 13. 완료 수용 기준

### 디자인

- 첫 화면에서 캐릭터, 오늘 누적, Active 4의 위계가 3초 안에 이해된다.
- 흰 카드가 반복되던 기존 인상에서 벗어나지만 데이터 가독성은 낮아지지 않는다.
- 핑크=대기/안내, 블루=실행/반응의 의미가 화면 전체에서 일관된다.
- Dribbble의 분위기는 느껴지되 레이아웃·캐릭터·보상 체계를 복제하지 않는다.
- 한 viewport에 큰 캐릭터가 둘 이상 경쟁하지 않는다.

### 기능

- 기존 상태 JSON을 마이그레이션 없이 그대로 불러온다.
- 시작, 종료, 전환, 수동 기록, 수정, 삭제, 복구가 이전과 동일하다.
- 7일/30일 통계와 Category 상세 값이 리뉴얼 전과 일치한다.
- AI가 실패해도 로컬 리플렉션과 기록 기능이 정상이다.
- 키보드, `window.habitToy`, Web Serial 입력과 LED 출력이 유지된다.
- 오프라인 PWA에서 캐릭터를 포함한 화면이 깨지지 않는다.

### 품질

- 320px에서 가로 페이지 스크롤이 없다. 단, 30일 히트맵 내부 스크롤은 허용한다.
- 캐릭터 이미지 로딩으로 레이아웃이 이동하지 않는다.
- 실행 중 1초 갱신 때문에 캐릭터 애니메이션이 반복 초기화되지 않는다.
- 캐릭터 상태 변경이 매초 새로운 스크린리더 알림을 만들지 않는다.
- reduced-motion에서 반복 애니메이션이 없다.
- 모든 핵심 컨트롤이 키보드로 접근 가능하고 포커스가 보인다.
- `npm run check`와 `npm test`가 통과한다.

## 14. 구현 시작 시 기본 결정안

추가 지시가 없다면 다음을 기본값으로 삼는다.

1. 브랜드명과 화면 문구는 현재 `Habit Toy`를 유지한다.
2. 첨부된 두 PNG를 canonical source로 사용한다.
3. 크림 + 라임을 기본 배경 축으로, 핑크/블루를 캐릭터 상태 축으로 사용한다.
4. 새 점수·보상·스트릭 기능은 추가하지 않는다.
5. 1차 구현은 Vanilla HTML/CSS/JS와 원본 PNG만 사용한다.
6. Higgsfield 및 추가 이미지 생성은 보류한다.
7. 기능 로직과 시각 리뉴얼은 가능한 한 분리된 변경 단위로 구현한다.
