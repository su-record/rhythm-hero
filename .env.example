# Keep this file as a template only. Never commit a real key.
OPENAI_API_KEY=
# Override only when your OpenAI project uses a different available model.
OPENAI_MODEL=gpt-5.6-terra
# Speech model and voice for the toy (alloy, ash, coral, echo, fable, nova, sage, shimmer).
OPENAI_TTS_MODEL=gpt-4o-mini-tts
OPENAI_TTS_VOICE=nova
